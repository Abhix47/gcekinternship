# Edge Impulse Library

The Edge Impulse Arduino library used during the internship has been omitted from this repository because it contains thousands of generated source files and exceeds Windows Git path-length limits.

The library can be regenerated by exporting the model from Edge Impulse and importing it into the PlatformIO project.